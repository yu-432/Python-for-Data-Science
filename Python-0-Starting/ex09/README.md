# Exercice 09: My first package creation

ビルド用ツールをインストール
`pip install build`
# または uv pip install build

ビルドを実行:
`python -m build`

ビルドしたパッケージをインストール
`pip install ./dist/ft_package-0.0.1.tar.gz`
`pip install ./dist/ft_package-0.0.1-py3-none-any.whl`